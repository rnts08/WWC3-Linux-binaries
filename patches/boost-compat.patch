diff --git a/src/rpcprotocol.h b/src/rpcprotocol.h
index adddc74..3af9fc5 100644
--- a/src/rpcprotocol.h
+++ b/src/rpcprotocol.h
@@ -104,7 +104,7 @@ public:
     }
     bool connect(const std::string& server, const std::string& port)
     {
-        boost::asio::ip::tcp::resolver resolver(stream.get_io_service());
+        boost::asio::ip::tcp::resolver resolver(stream.get_executor());
         boost::asio::ip::tcp::resolver::query query(server.c_str(), port.c_str());
         boost::asio::ip::tcp::resolver::iterator endpoint_iterator = resolver.resolve(query);
         boost::asio::ip::tcp::resolver::iterator end;
diff --git a/src/rpcserver.cpp b/src/rpcserver.cpp
index 6dc2e11..dad8952 100644
--- a/src/rpcserver.cpp
+++ b/src/rpcserver.cpp
@@ -383,6 +383,17 @@ template <typename Protocol>
 class AcceptedConnectionImpl : public AcceptedConnection
 {
 public:
+#if BOOST_VERSION >= 106600
+    AcceptedConnectionImpl(
+            const boost::asio::any_io_executor& executor,
+            ssl::context &context,
+            bool fUseSSL) :
+        sslStream(executor, context),
+        _d(sslStream, fUseSSL),
+        _stream(_d)
+    {
+    }
+#else
     AcceptedConnectionImpl(
             asio::io_service& io_service,
             ssl::context &context,
@@ -392,6 +403,7 @@ public:
         _stream(_d)
     {
     }
+#endif
 
     virtual std::iostream& stream()
     {
@@ -434,8 +446,13 @@ static void RPCListen(boost::shared_ptr< basic_socket_acceptor<Protocol, SocketA
                    ssl::context& context,
                    const bool fUseSSL)
 {
-    // Accept connection
+    // Accept connection - use get_executor() for new Boost, fallback to get_io_service()
+    // Both return something compatible with io_service for AcceptedConnectionImpl
+#if BOOST_VERSION >= 106600
+    AcceptedConnectionImpl<Protocol>* conn = new AcceptedConnectionImpl<Protocol>(acceptor->get_executor(), context, fUseSSL);
+#else
     AcceptedConnectionImpl<Protocol>* conn = new AcceptedConnectionImpl<Protocol>(acceptor->get_io_service(), context, fUseSSL);
+#endif
 
     acceptor->async_accept(
             conn->sslStream.lowest_layer(),
@@ -508,7 +525,7 @@ void StartRPCThreads()
 
     assert(rpc_io_service == NULL);
     rpc_io_service = new asio::io_service();
-    rpc_ssl_context = new ssl::context(*rpc_io_service, ssl::context::sslv23);
+    rpc_ssl_context = new ssl::context(ssl::context::sslv23);
 
     const bool fUseSSL = GetBoolArg("-rpcssl", false);
 
@@ -527,7 +544,7 @@ void StartRPCThreads()
         else LogPrintf("ThreadRPCServer ERROR: missing server private key file %s\n", pathPKFile.string());
 
         string strCiphers = GetArg("-rpcsslciphers", "TLSv1.2+HIGH:TLSv1+HIGH:!SSLv2:!aNULL:!eNULL:!3DES:@STRENGTH");
-        SSL_CTX_set_cipher_list(rpc_ssl_context->impl(), strCiphers.c_str());
+        SSL_CTX_set_cipher_list(rpc_ssl_context->native_handle(), strCiphers.c_str());
     }
 
     // Try a dual IPv6/IPv4 socket, falling back to separate IPv4 and IPv6 sockets
