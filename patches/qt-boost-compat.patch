diff --git a/src/main.cpp b/src/main.cpp
index b811ed5..668f449 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -6,6 +6,8 @@
 #include <boost/algorithm/string/replace.hpp>
 #include <boost/filesystem.hpp>
 #include <boost/filesystem/fstream.hpp>
+#include <boost/bind.hpp>
+using namespace boost::placeholders;
 
 #include "chainparams.h"
 #include "checkpoints.h"
diff --git a/src/qt/clientmodel.cpp b/src/qt/clientmodel.cpp
index 1234567..abcdefg 100644
--- a/src/qt/clientmodel.cpp
+++ b/src/qt/clientmodel.cpp
@@ -1,6 +1,8 @@
 #include "clientmodel.h"
 
+#include <boost/bind.hpp>
+using namespace boost::placeholders;
+
 #include "guiconstants.h"
 #include "optionsmodel.h"
 #include "addresstablemodel.h"
diff --git a/src/qt/walletmodel.cpp b/src/qt/walletmodel.cpp
index 1234567..abcdefg 100644
--- a/src/qt/walletmodel.cpp
+++ b/src/qt/walletmodel.cpp
@@ -1,6 +1,8 @@
 #include "walletmodel.h"
 
+#include <boost/bind.hpp>
+using namespace boost::placeholders;
+
 #include "guiconstants.h"
 #include "optionsmodel.h"
 #include "addresstablemodel.h"
diff --git a/src/rpcclient.cpp b/src/rpcclient.cpp
index a383e6a..fef3d98 100644
--- a/src/rpcclient.cpp
+++ b/src/rpcclient.cpp
@@ -35,9 +35,9 @@ Object CallRPC(const string& strMethod, const Array& params)
 
     // Connect to localhost
     bool fUseSSL = GetBoolArg("-rpcssl", false);
-    asio::io_service io_service;
-    ssl::context context(io_service, ssl::context::sslv23);
+    ssl::context context(ssl::context::sslv23);
     context.set_options(ssl::context::no_sslv2);
+    asio::io_service io_service;
     asio::ssl::stream<asio::ip::tcp::socket> sslStream(io_service, context);
-    SSLIOStreamDevice<asio::ip::tcp> d(sslStream, fUseSSL);
+    SSLIOStreamDevice<asio::ip::tcp> d(io_service, sslStream, fUseSSL);
     iostreams::stream< SSLIOStreamDevice<asio::ip::tcp> > stream(d);
diff --git a/src/rpcprotocol.h b/src/rpcprotocol.h
index 1234567..abcdefg 100644
--- a/src/rpcprotocol.h
+++ b/src/rpcprotocol.h
@@ -78,7 +78,8 @@ template <typename Protocol>
 class SSLIOStreamDevice : public boost::iostreams::device<boost::iostreams::bidirectional> {
 public:
-    SSLIOStreamDevice(boost::asio::ssl::stream<typename Protocol::socket> &streamIn, bool fUseSSLIn) : stream(streamIn)
+    SSLIOStreamDevice(boost::asio::io_service& ioServiceIn, boost::asio::ssl::stream<typename Protocol::socket> &streamIn, bool fUseSSLIn) : ioService(ioServiceIn), stream(streamIn)
     {
         fUseSSL = fUseSSLIn;
         fNeedHandshake = fUseSSLIn;
@@ -104,7 +105,7 @@ public:
     bool connect(const std::string& server, const std::string& port)
     {
-        boost::asio::ip::tcp::resolver resolver(stream.get_io_service());
+        boost::asio::ip::tcp::resolver resolver(ioService);
         boost::asio::ip::tcp::resolver::query query(server.c_str(), port.c_str());
         boost::asio::ip::tcp::resolver::iterator endpoint_iterator = resolver.resolve(query);
         boost::asio::ip::tcp::resolver::iterator end;
@@ -121,6 +122,7 @@ private:
     bool fNeedHandshake;
     bool fUseSSL;
+    boost::asio::io_service& ioService;
     boost::asio::ssl::stream<typename Protocol::socket>& stream;
 };
diff --git a/src/rpcserver.cpp b/src/rpcserver.cpp
index 1234567..abcdefg 100644
--- a/src/rpcserver.cpp
+++ b/src/rpcserver.cpp
@@ -385,8 +385,9 @@ public:
             asio::io_service& io_service,
             ssl::context &context,
             bool fUseSSL) :
         sslStream(io_service, context),
-        _d(sslStream, fUseSSL),
+        _d(io_service, sslStream, fUseSSL),
         _stream(_d)
     {
     }
@@ -421,14 +422,18 @@ void ServiceConnection(AcceptedConnection *conn);
 // Forward declaration required for RPCListen
 template <typename Protocol, typename SocketAcceptorService>
-static void RPCAcceptHandler(boost::shared_ptr< basic_socket_acceptor<Protocol, SocketAcceptorService> > acceptor,
+static void RPCAcceptHandler(asio::io_service& ioService,
+                             boost::shared_ptr< basic_socket_acceptor<Protocol, SocketAcceptorService> > acceptor,
                              ssl::context& context,
                              bool fUseSSL,
                              AcceptedConnection* conn,
                              const boost::system::error_code& error);
 
+// Fix ssl::context constructor
+rpc_ssl_context = new ssl::context(ssl::context::sslv23);
+
 /**
  * Sets up I/O resources to accept and handle a new connection.
  */
@@ -438,8 +443,10 @@ static void RPCListen(asio::io_service& ioService,
                    const bool fUseSSL)
 {
     // Accept connection
-    AcceptedConnectionImpl<Protocol>* conn = new AcceptedConnectionImpl<Protocol>(acceptor->get_io_service(), context, fUseSSL);
+    AcceptedConnectionImpl<Protocol>* conn = new AcceptedConnectionImpl<Protocol>(ioService, context, fUseSSL);
 
+    // Pass ioService to handler
     acceptor->async_accept(
             conn->sslStream.lowest_layer(),
             conn->peer,
             boost::bind(&RPCAcceptHandler<Protocol, SocketAcceptorService>,
+                boost::ref(ioService),
                 acceptor,
                 boost::ref(context),
                 fUseSSL,
@@ -467,7 +474,7 @@ static void RPCAcceptHandler(...)
 {
     // Immediately start accepting new connections, except when we're cancelled or our socket is closed.
     if (error != asio::error::operation_aborted && acceptor->is_open())
-        RPCListen(acceptor, context, fUseSSL);
+        RPCListen(ioService, acceptor, context, fUseSSL);
 
     AcceptedConnectionImpl<ip::tcp>* tcp_conn = dynamic_cast< AcceptedConnectionImpl<ip::tcp>* >(conn);
@@ -512,7 +519,7 @@ void StartRPCThreads()
     assert(rpc_io_service == NULL);
     rpc_io_service = new asio::io_service();
-    rpc_ssl_context = new ssl::context(*rpc_io_service, ssl::context::sslv23);
+    rpc_ssl_context = new ssl::context(ssl::context::sslv23);
 
     const bool fUseSSL = GetBoolArg("-rpcssl", false);
 
@@ -530,7 +537,7 @@ void StartRPCThreads()
 
         string strCiphers = GetArg("-rpcsslciphers", "TLSv1.2+HIGH:TLSv1+HIGH:!SSLv2:!aNULL:!eNULL:!3DES:@STRENGTH");
-        SSL_CTX_set_cipher_list(rpc_ssl_context->impl(), strCiphers.c_str());
+        SSL_CTX_set_cipher_list(rpc_ssl_context->native_handle(), strCiphers.c_str());
     }
 
     // Try a dual IPv6/IPv4 socket, falling back to separate IPv4 and IPv6 sockets
@@ -556,7 +563,7 @@ void StartRPCThreads()
         acceptor->bind(endpoint);
         acceptor->listen(socket_base::max_connections);
 
-        RPCListen(acceptor, *rpc_ssl_context, fUseSSL);
+        RPCListen(*rpc_io_service, acceptor, *rpc_ssl_context, fUseSSL);
 
         fListening = true;
     }
@@ -578,7 +585,7 @@ void StartRPCThreads()
             acceptor->bind(endpoint);
             acceptor->listen(socket_base::max_connections);
 
-            RPCListen(acceptor, *rpc_ssl_context, fUseSSL);
+            RPCListen(*rpc_io_service, acceptor, *rpc_ssl_context, fUseSSL);
 
             fListening = true;
         }
diff --git a/src/rpcwallet.cpp b/src/rpcwallet.cpp
index 1234567..abcdefg 100644
--- a/src/rpcwallet.cpp
+++ b/src/rpcwallet.cpp
@@ -1700,7 +1700,7 @@ static json_spirit::Array GetStakeReport()
     int64_t nToday = GetTime();
     time_t CurTime = nToday;
 
-    localtime_r(&CurTime, &Loc_MidNight);
+    localtime_s(&Loc_MidNight, &CurTime);
     Loc_MidNight.tm_hour = 0;
     Loc_MidNight.tm_min = 0;
     Loc_MidNight.tm_sec = 0;  // set midnight
